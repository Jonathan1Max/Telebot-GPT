version: '3.8'

services:
  telegram-java:
    image: telegram-app-1
    build: .
    ports:
      - "443:80"  # Cambia este puerto al 80 o 443
    networks:
      - red-external-network
    restart: always

networks:
  red-external-network:
    external: true
